from django.apps import AppConfig


class AIPriceHubConfig(AppConfig):
    """App configuration for AI Price Hub."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "ai_pricehub"
    verbose_name = "AI Price Hub"
